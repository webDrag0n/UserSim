[project]
name = "usersim"
version = "0.1.0"
description = "长程用户-手机助手模拟与控制论 Benchmark 系统"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.7",
    "numpy>=1.26",
    "fastapi>=0.110",
    "uvicorn>=0.29",
    "openai>=1.30",
]

[dependency-groups]
dev = [
    "pytest>=8",
]
